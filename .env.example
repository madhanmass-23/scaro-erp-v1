# =================================================================
# SCARO ERP — Frontend Production Environment Configuration Template
# =================================================================

# Backend API Base URL (Node.js/Express + MariaDB)
# For same-origin single-server deployment (recommended): /api/v1
# For separated frontend/backend hosts: https://erp.scarotech.in/api/v1
VITE_API_BASE_URL=/api/v1
